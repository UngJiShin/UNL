# Google Gemini TTS
GEMINI_API_KEY=

# 데이터베이스 (프로덕션). 미설정 시 인메모리 스토어로 폴백.
DATABASE_URL=

# 캐시 (프로덕션). 미설정 시 인메모리 LRU 캐시로 폴백.
REDIS_URL=

# 오디오 스토리지
AWS_S3_BUCKET=
AWS_CLOUDFRONT_DOMAIN=

# 음소 파서 외부 마이크로서비스(선택). 미설정 시 내장 TS 파서 사용.
PHONEME_SERVICE_URL=
